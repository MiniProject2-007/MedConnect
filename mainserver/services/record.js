class RecordService {
    createRecord = async ({
        ownerId,
        sharedWith,
        title,
        description,
        recordType,
        file,
        status,
    }) => {
        // try{

        // }
    };
}
